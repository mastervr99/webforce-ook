{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.lastname) }}
    </div>
    <div class="form-group">
        {{ form_row(form.firstname) }}
    </div>
    <div class="form-group">
        {{ form_row(form.email) }}
    </div>
    <div class="form-group">
        {{ form_row(form.postalCode) }}
    </div>
    <div class="form-group">
        {{ form_row(form.city) }}
    </div>
    <div class="form-group">
        {{ form_row(form.emploi) }}
    </div>

    <div class="form-group">
        <button class="btn-valider btn-block" type="submit">Rechercher</button>
    </div>
    {{ form_end(form) }}


    <div class="container">
        <ul class="list-group list-group-flush">
            {% for user  in users %}
                <li class="list-group-item">
                    <div class="contact" id="contact">
                        <div class="row">
                            <div class="col-sm-7 col-md-4  d-flex align-items-center">
                                <div class="d-flex align-items-center">
                                    <div class="text">
                                        {{ user.lastname }} {{ user.firstname }}
                                        <br>
                                        <small> {{ user.emploi }}</small>
                                    </div>
                                </div>
                            </div>

                            <div class="col-sm-1 col-md-1 d-flex justify-content-center mx-auto my-auto">
                                <a class="btn btn-link btn-info nav-link mr-3" href=""><i class="fas fa-pen"></i></a>
                            </div>

                        </div>
                    </div>
                </li>
            {% else %}
                <div class="alert alert-info mt-5">Aucun utilisateur trouvé</div>
            {% endfor %}
        </ul>
    </div>
{% endblock %}

